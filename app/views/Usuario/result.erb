<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Admin</title>
  <style type="text/css">
      body{
          background-color: #1450C0;
          padding: 0;
          margin: 0;
          font-family: arial;
        }
        #menu{
          background-color: #000;

        }
        #menu ul{
            margin: 0;
            padding: 0;
            list-style: none;
        }
        #menu ul li{
          display: inline-block;
        }
        #menu ul li a{
          color: white;
          display: block;
          padding: 10px 20px;
          text-decoration: none;

        }
        #menu ul li a:hover{
          background-color: #0356F3 ;
        }

        .item-r{
          float: right;
        }
        #user{
          background-color: black;
        }
        #user li{
          color: #03E7BD;
          display: inline-block;
          text-decoration: none;
        }
        .item-s1{
          margin-top: 5px;
          margin-left: 20px;
        }
        .item-s2{
          float: right;
          margin-top: 5px;
          margin-right: 30px;
        }

        #main-container{
          width: 600px;
          margin: 150px auto;
        }
        table{
          width: 100%;
          background-color: white;
          text-align: left;
          border-collapse: collapse;
        }
        th,td{
          border: solid 5px black ;
          padding: 20px;
        }
        thead{
          border-bottom: solid 10px #blue;
          color: black;
        }
        tr{
          background-color: #ddd;

        }
        h1{
          margin: 50px auto;
          text-align: center;
        }
        .principal{
          text-align: center;
        }
        .principal tr{
          background-color: #2E2E2E;
          font-size: 25px;
          color: green;
        }
        .secundaria a{
          text-decoration: none;
          color: black;
        }

        


  </style>
</head>
<body>
  <div id="user">
    <li class="item-s1">Usuario: <%= @current_user.username %></li>
    <li class="item-s2">Puntos: <%= @current_user.point %></li>
    <li class="item-s2">Racha: <%= @current_user.streak %></li>
  </div>
  </ul>
  <div id="menu">
    <ul>
      <li><a href="http://localhost:4567/login/home">Home</a></li>
      <li><a href="http://localhost:4567/login/home/play">Play</a></li>
      <li><a href="http://localhost:4567/login/ranking">Ranking</a></li>
      <li><a href="http://localhost:4567/login/history">History</a></li>
      <li class="item-r"><a href="http://localhost:4567/">Logout</a></li>
    </ul>
  </div>
    <main>
    <% if (@tournament != nil)%>
    <h1 for='Team'>Torneo "<%=@tournament.first.tournament.name%>"</h1>
    <div id="main-container">
      <table class="principal">
        <thead>
          <tr>
            <th>Partidos</th>
          </tr>
        </thead>
        <% @tournament.each do |algo| %>
        <table class="secundaria">
          <tr class="titulo">
            <th><a href="http://localhost:4567/login/game?id_match=<%= algo.match.id %>" class="viendo2"><%="#{algo.match.local.name} vs #{algo.match.visitor.name}"%></a></th>
          </tr> 
        </table>
          <% end %>
      </table>
    </div>
    <% else %>
      <div class="success"><%= flash[:error]%></div>
    <% end %>
  </main>
</body>
</html>


